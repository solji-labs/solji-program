/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { TEMPLE_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const DRAW_FORTUNE_DISCRIMINATOR = new Uint8Array([
  167, 57, 67, 163, 76, 93, 229, 97,
]);

export function getDrawFortuneDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DRAW_FORTUNE_DISCRIMINATOR
  );
}

export type DrawFortuneInstruction<
  TProgram extends string = typeof TEMPLE_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountUserState extends string | AccountMeta<string> = string,
  TAccountTempleConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountUserState extends string
        ? WritableAccount<TAccountUserState>
        : TAccountUserState,
      TAccountTempleConfig extends string
        ? ReadonlyAccount<TAccountTempleConfig>
        : TAccountTempleConfig,
      ...TRemainingAccounts,
    ]
  >;

export type DrawFortuneInstructionData = {
  discriminator: ReadonlyUint8Array;
  useMerit: boolean;
};

export type DrawFortuneInstructionDataArgs = { useMerit: boolean };

export function getDrawFortuneInstructionDataEncoder(): FixedSizeEncoder<DrawFortuneInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['useMerit', getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: DRAW_FORTUNE_DISCRIMINATOR })
  );
}

export function getDrawFortuneInstructionDataDecoder(): FixedSizeDecoder<DrawFortuneInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['useMerit', getBooleanDecoder()],
  ]);
}

export function getDrawFortuneInstructionDataCodec(): FixedSizeCodec<
  DrawFortuneInstructionDataArgs,
  DrawFortuneInstructionData
> {
  return combineCodec(
    getDrawFortuneInstructionDataEncoder(),
    getDrawFortuneInstructionDataDecoder()
  );
}

export type DrawFortuneAsyncInput<
  TAccountUser extends string = string,
  TAccountUserState extends string = string,
  TAccountTempleConfig extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userState?: Address<TAccountUserState>;
  templeConfig?: Address<TAccountTempleConfig>;
  useMerit: DrawFortuneInstructionDataArgs['useMerit'];
};

export async function getDrawFortuneInstructionAsync<
  TAccountUser extends string,
  TAccountUserState extends string,
  TAccountTempleConfig extends string,
  TProgramAddress extends Address = typeof TEMPLE_PROGRAM_ADDRESS,
>(
  input: DrawFortuneAsyncInput<
    TAccountUser,
    TAccountUserState,
    TAccountTempleConfig
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  DrawFortuneInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserState,
    TAccountTempleConfig
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? TEMPLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userState: { value: input.userState ?? null, isWritable: true },
    templeConfig: { value: input.templeConfig ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.userState.value) {
    accounts.userState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([117, 115, 101, 114, 95, 115, 116, 97, 116, 101])
        ),
        getAddressEncoder().encode(expectAddress(accounts.user.value)),
      ],
    });
  }
  if (!accounts.templeConfig.value) {
    accounts.templeConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([116, 101, 109, 112, 108, 101, 95, 118, 49])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userState),
      getAccountMeta(accounts.templeConfig),
    ],
    programAddress,
    data: getDrawFortuneInstructionDataEncoder().encode(
      args as DrawFortuneInstructionDataArgs
    ),
  } as DrawFortuneInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserState,
    TAccountTempleConfig
  >;

  return instruction;
}

export type DrawFortuneInput<
  TAccountUser extends string = string,
  TAccountUserState extends string = string,
  TAccountTempleConfig extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userState: Address<TAccountUserState>;
  templeConfig: Address<TAccountTempleConfig>;
  useMerit: DrawFortuneInstructionDataArgs['useMerit'];
};

export function getDrawFortuneInstruction<
  TAccountUser extends string,
  TAccountUserState extends string,
  TAccountTempleConfig extends string,
  TProgramAddress extends Address = typeof TEMPLE_PROGRAM_ADDRESS,
>(
  input: DrawFortuneInput<
    TAccountUser,
    TAccountUserState,
    TAccountTempleConfig
  >,
  config?: { programAddress?: TProgramAddress }
): DrawFortuneInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountUserState,
  TAccountTempleConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? TEMPLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userState: { value: input.userState ?? null, isWritable: true },
    templeConfig: { value: input.templeConfig ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userState),
      getAccountMeta(accounts.templeConfig),
    ],
    programAddress,
    data: getDrawFortuneInstructionDataEncoder().encode(
      args as DrawFortuneInstructionDataArgs
    ),
  } as DrawFortuneInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserState,
    TAccountTempleConfig
  >;

  return instruction;
}

export type ParsedDrawFortuneInstruction<
  TProgram extends string = typeof TEMPLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    userState: TAccountMetas[1];
    templeConfig: TAccountMetas[2];
  };
  data: DrawFortuneInstructionData;
};

export function parseDrawFortuneInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedDrawFortuneInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      userState: getNextAccount(),
      templeConfig: getNextAccount(),
    },
    data: getDrawFortuneInstructionDataDecoder().decode(instruction.data),
  };
}
