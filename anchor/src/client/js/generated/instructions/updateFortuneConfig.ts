/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { TEMPLE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getFortuneConfigDecoder,
  getFortuneConfigEncoder,
  type FortuneConfig,
  type FortuneConfigArgs,
} from '../types';

export const UPDATE_FORTUNE_CONFIG_DISCRIMINATOR = new Uint8Array([
  103, 193, 204, 242, 205, 62, 148, 221,
]);

export function getUpdateFortuneConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_FORTUNE_CONFIG_DISCRIMINATOR
  );
}

export type UpdateFortuneConfigInstruction<
  TProgram extends string = typeof TEMPLE_PROGRAM_ADDRESS,
  TAccountTempleConfig extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTempleConfig extends string
        ? WritableAccount<TAccountTempleConfig>
        : TAccountTempleConfig,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateFortuneConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  regularFortune: FortuneConfig;
  buddhaFortune: FortuneConfig;
};

export type UpdateFortuneConfigInstructionDataArgs = {
  regularFortune: FortuneConfigArgs;
  buddhaFortune: FortuneConfigArgs;
};

export function getUpdateFortuneConfigInstructionDataEncoder(): FixedSizeEncoder<UpdateFortuneConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['regularFortune', getFortuneConfigEncoder()],
      ['buddhaFortune', getFortuneConfigEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_FORTUNE_CONFIG_DISCRIMINATOR,
    })
  );
}

export function getUpdateFortuneConfigInstructionDataDecoder(): FixedSizeDecoder<UpdateFortuneConfigInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['regularFortune', getFortuneConfigDecoder()],
    ['buddhaFortune', getFortuneConfigDecoder()],
  ]);
}

export function getUpdateFortuneConfigInstructionDataCodec(): FixedSizeCodec<
  UpdateFortuneConfigInstructionDataArgs,
  UpdateFortuneConfigInstructionData
> {
  return combineCodec(
    getUpdateFortuneConfigInstructionDataEncoder(),
    getUpdateFortuneConfigInstructionDataDecoder()
  );
}

export type UpdateFortuneConfigInput<
  TAccountTempleConfig extends string = string,
  TAccountAuthority extends string = string,
> = {
  templeConfig: Address<TAccountTempleConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  regularFortune: UpdateFortuneConfigInstructionDataArgs['regularFortune'];
  buddhaFortune: UpdateFortuneConfigInstructionDataArgs['buddhaFortune'];
};

export function getUpdateFortuneConfigInstruction<
  TAccountTempleConfig extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof TEMPLE_PROGRAM_ADDRESS,
>(
  input: UpdateFortuneConfigInput<TAccountTempleConfig, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): UpdateFortuneConfigInstruction<
  TProgramAddress,
  TAccountTempleConfig,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? TEMPLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    templeConfig: { value: input.templeConfig ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.templeConfig),
      getAccountMeta(accounts.authority),
    ],
    programAddress,
    data: getUpdateFortuneConfigInstructionDataEncoder().encode(
      args as UpdateFortuneConfigInstructionDataArgs
    ),
  } as UpdateFortuneConfigInstruction<
    TProgramAddress,
    TAccountTempleConfig,
    TAccountAuthority
  >;

  return instruction;
}

export type ParsedUpdateFortuneConfigInstruction<
  TProgram extends string = typeof TEMPLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    templeConfig: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: UpdateFortuneConfigInstructionData;
};

export function parseUpdateFortuneConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateFortuneConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      templeConfig: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getUpdateFortuneConfigInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
